import { Routes } from '@angular/router';
import { SearchPageComponent } from './ui/smart/search-page/search-page.component';

export const routes: Routes = [{ path: '', component: SearchPageComponent }];
