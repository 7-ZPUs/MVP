// Driven port interfaces (repositories and external services implemented by infrastructure).
// Each interface should be placed in its own file and re-exported here.
// Example: export type { IPackageRepository } from './IPackageRepository';
export {};
